# Synthetic public sample

This folder is a deliberately fictional, sanitized Pipeline V2 example. `Example Group`, `Testland`, all values, and the `example.invalid` source are test fixtures; they are not research findings.

The sample demonstrates canonical Source → Observation → Fact → Report Data → Dashboard lineage and a passing deterministic Quality summary. It contains no local path, credential, session, or real company claim.
