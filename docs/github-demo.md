# GitHub demo

Use `examples/sample_run/` for public demonstrations. The sample must be synthetic or explicitly sanitized and must not contain usernames, absolute local paths, credentials, cookies, sessions, or unrelated logs.

Before publication run Python tests, Fake Agent E2E, dashboard tests/build, `python -m tools.repository_check`, and inspect `git status` plus the staged diff. Do not publish the full `outputs/` tree. A repository license still requires an explicit owner decision.
